;
; BIND data file for grifinoria.br zone
;
$TTL    604800
@       IN      SOA     ns.grifinoria.br. root.grifinoria.br. (
                              4         ; Serial (incrementado)
                         604800         ; Refresh
                          86400         ; Retry
                        2419200         ; Expire
                         604800 )       ; Negative Cache TTL

; Nameserver
        IN      NS      ns
ns      IN      A       192.168.1.65

; Domínio raiz e www
@       IN      A       192.168.1.65    ; grifinoria.br
www     IN      A       192.168.1.65     ; www.grifinoria.br

; Serviços via CNAME para o apex
sign        IN      CNAME   @               ; sign.grifinoria.br → 192.168.0.9
hotsite     IN      CNAME   @               ; hotsite.grifinoria.br → 192.168.0.9
proxy       IN      CNAME   @               ; proxy.grifinoria.br → 192.168.0.9
portal      IN      CNAME   @               ; portal.grifinoria.br → 192.168.0.9