;
; BIND data file for grifinoria.br zone
;
$TTL    604800
@       IN      SOA     ns.grifinoria.br. root.grifinoria.br. (
                              2         ; Serial (incrementado)
                         604800         ; Refresh
                          86400         ; Retry
                        2419200         ; Expire
                         604800 )       ; Negative Cache TTL

; Nameserver
        IN      NS      ns
ns      IN      A       192.168.1.65

; Domínio raiz e www
@       IN      A       192.168.1.65    ; grifinoria.br
www     IN      A       192.168.1.65     ; www.grifinoria.br

; Serviços via CNAME para o apex
sign        IN      CNAME   @               ; portal.grifinoria.br → 192.168.0.9
hotsite     IN      CNAME   @               ; webmail.grifinoria.br → 192.168.0.9
proxy       IN      CNAME   @               ; sonserina.grifinoria.br → 192.168.0.9
portal      IN      CNAME   @               ; sonserina.grifinoria.br → 192.168.0.9